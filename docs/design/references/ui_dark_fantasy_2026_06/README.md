# Референсы: Dark Fantasy UI (решение пользователя 2026-06-12)

Сюда пользователь выкладывает 5 референсов кнопок из PM-чата (2026-06-12).
Без этих файлов генерация по задаче
`docs/tasks/design_codex_ui_dark_fantasy_restyle_task.md` НЕ запускается
(железное правило арта: референсы-изображения прикладываются к каждой генерации).

Ожидаемые файлы (имена — по содержанию листов):

1. `button_parchment_wax_seal.png` — Parchment & Wax Seal: пергамент в тёмной
   металлической оправе с шипами, красная сургучная печать слева, рубин справа;
   состояния Idle / Hover (тёплое свечение) / Pressed (затемнение) / Disabled
   (обесцвечивание).
2. `button_dragon_scale_v4.png` — Dragon-Scale: красная драконья чешуя,
   когтистые стальные лапы по краям, красные камни-глаза; 4 состояния.
3. `button_obsidian_brass_runed.png` — Dark Fantasy Obsidian & Brass: чёрный
   обсидиан с трещинами, латунная окантовка, янтарные руны по углам, ember glow
   на hover/pressed; 4 состояния.
4. `button_dwarven_stone_v9.png` — Dwarven Stone: серый камень с гравировкой,
   массивная латунно-золотая рамка с геометрическим дварфийским орнаментом;
   4 состояния.
5. `button_necromancer_v8.png` — Necromancer: чёрный камень, тонкая стальная
   оправа с шипами и черепом сверху, зелёные кристаллы и некро-свечение;
   4 состояния.

Общие черты канона (зафиксировать в visual_style_assets.md при старте задачи):
тёмный фон, painterly realism, материальность (металл/камень/кожа/пергамент),
угловые орнаменты-акценты, единая схема состояний Idle/Hover/Pressed/Disabled
(hover = свечение/подсветка, pressed = затемнение+сжатие рамки, disabled =
десатурация).
