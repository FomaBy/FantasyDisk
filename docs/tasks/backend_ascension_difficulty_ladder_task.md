# Задача Для Back-end-Агента: Возвышения 2.0 — Лестница Из 10 Кумулятивных Усложнений

Статус: done 2026-06-12. Результат: (1) data-driven ASCENSION_MODIFIERS (10 кумулятивных усложнений) + ascension_difficulty_mods(level) в progression_data; (2) разблокировка по персонажу через meta_progression.record_boss_victory(run_level) — открывает N+1 только при беге на текущем максимуме, selectable_max = completed+1; (3) старые ASCENSION_LEVELS перевешены в наградный трек меты (apply_ascension_bonuses применяет earned-баффы за пройденные уровни + difficulty trophies/healing/max-HP в run_modifiers), мета-экран после босса — заглушка с рабочим хуком; (4) селектор уровня в hero select (−/+, кумулятивный список усложнений, клампится к максимуму героя при пике, бейдж на карточке) + HUD-индикатор римской цифрой у таймера + раздел «Возвышения» в кодексе; (5) применение в бою через game.ascension_difficulty(): enemy hp/damage, elite hp/instant-phase, boss hp/extra-phase(4-я фаза в boss.gd)/короче-телеграфы, spawn density/rate + first-wave boost, round duration, цены магазина/докачки; уровень 0 не меняет числа. Координация с enemy-scaling: difficulty УМНОЖАЕТ stage_scale, не компенсирует (точки применения поверх его множителей). Тесты: 10 модификаторов, нейтраль уровня 0, кумулятивность (3 = 1+2+3, без 4+), разблокировка по персонажу + гейтинг по run_level, применение (цены растут на L2, макс-HP падает на L10); все 4 smoke зелёные, runtime стабильно. ПРИМЕЧАНИЕ: значения тюнились поверх текущей незакоммиченной enemy-scaling работы — финальный баланс согласовать на совместном проходе (как и просил PM).
Создано: 2026-06-12
Автор: PM
Масштаб: крупная система. Координация: тюнить значения ПОСЛЕ/вместе с
`backend_enemy_scaling_elite_boss_difficulty_task.md` (возвышения умножают
уже новую сложность, не компенсируют старую).

## Autonomy / Approval
Пользователь заранее одобрил. Список 10 усложнений ниже — направление PM,
улучшай формулировки/числа, состав согласован с пользователем — не менять без причины.

## Контекст (решение пользователя)
Возвышение — режим усложнения как «heat» в Hades / Ascension в Slay the Spire:
1. 10 уровней; уровень N включает ВСЕ модификаторы уровней 1..N (кумулятивно).
2. Возвышения делают игру сложнее и ОТКРЫВАЮТ мета-прогрессию.
3. Мета-прогрессия выдается после убийства финального босса — ПОКА ЗАГЛУШКА,
   но хук должен работать.
4. На экране выбора персонажа каждый герой может начать забег на любом
   ДОСТУПНОМ ему уровне возвышения (0..открытый максимум; прогресс — по персонажу).

ВАЖНО — конфликт с текущей системой: сейчас ASCENSION_LEVELS — это БАФФЫ игрока
(+урон/+HP за уровень). Переработать: старые баффы НЕ выбрасывать, а перевесить
в наградный трек меты (см. ниже).

## 10 Возвышений (кумулятивные усложнения)

| # | Название | Эффект |
| --- | --- | --- |
| 1 | Закалённые враги | Монстры +15% HP и +10% урона |
| 2 | Жадные торговцы | Все цены (магазин/докачка/reroll) +25% |
| 3 | Быстрая орда | Спавн волн чаще, плотность +20% |
| 4 | Свирепые элитки | Элитки: +20% HP, вторая фаза открывается сразу |
| 5 | Скудные трофеи | Золото и опыт с боёв −20% |
| 6 | Истончённая плоть | Всё лечение (зелья/регенерация/вампиризм/drain) −30% |
| 7 | Эхо бездны | В обычных волнах может появиться мини-элитка со свитой |
| 8 | Длинная вахта | Таймер боя +25% (дольше выживать), спавн не ослабевает |
| 9 | Гнев стража | Босс: +1 фаза опасных зон, +20% HP, телеграфы короче |
| 10 | Грань безумия | Игрок: −20% макс. HP; стартовая волна каждого боя усилена |

## Требования
1. **Данные**: `ASCENSION_MODIFIERS` (10 записей выше) — глобальные, data-driven,
   кумулятивное применение (уровень N = сумма 1..N). Канонические ID и имена —
   в content_registry.
2. **Прогресс по персонажу**: переиспользовать существующий
   `meta_progression.gd` (levels per character уже хранится): победа над
   финальным боссом на уровне N → открывается N+1 для ЭТОГО персонажа.
3. **Мета-награда (заглушка с работающим хуком)**: при победе на возвышении N
   персонаж получает свой старый бафф `<класс>_asc_N` (бывшие ASCENSION_LEVELS —
   теперь это наградный трек меты, применяется на старте забега постоянно).
   Экран «мета-прогрессия» после босса — заглушка-уведомление «Открыто: …»
   (полноценный экран меты — будущая задача).
4. **Выбор уровня на старте**: в выборе персонажа (инфо-панель героя) — селектор
   уровня возвышения 0..открытый максимум персонажа, со списком активных
   модификаторов выбранного уровня (кумулятивный список читаемым текстом).
   Текущий уровень виден на карточке (например «Возвышение III»).
5. **Применение в бою**: модификаторы влияют на combat_director/enemy/boss/цены/
   лечение строго в момент забега; уровень 0 = ничего не меняет (текущая игра).
6. **HUD**: индикатор активного возвышения в забеге (римская цифра у таймера),
   тултип со списком модификаторов.
7. **Кодекс/доки**: раздел «Возвышения» в кодексе со всеми 10.
8. **Тесты**: кумулятивность (на уровне 3 активны 1+2+3), разблокировка после
   босса, награда-бафф выдается, селектор не дает выбрать закрытый уровень,
   уровень 0 не меняет числа; все smoke зеленые.

## Files / Assets / IDs
- `scripts/progression_data.gd` (ASCENSION_MODIFIERS, перевязка старых ASCENSION_LEVELS
  в наградный трек), `scripts/meta_progression.gd` (unlock/прогресс),
  `scripts/combat_director.gd`/`enemy.gd`/`boss.gd` (применение), `scripts/player.gd`
  (HP/лечение/баффы), `scripts/ui_screens.gd` (селектор в выборе героя, HUD-индикатор,
  заглушка меты), `scripts/codex_data.gd`.

## Acceptance Criteria
- [ ] 10 кумулятивных усложнений работают и применяются по выбранному уровню.
- [ ] Прогресс и разблокировка — по персонажу, через победу над финальным боссом.
- [ ] Старые asc-баффы стали наградным треком меты (хук работает, экран — заглушка).
- [ ] Селектор уровня в выборе героя + HUD-индикатор + кодекс.
- [ ] Тесты кумулятивности/разблокировки/нулевого уровня; smoke зеленые.

## Документация
- mechanics_extract (таблица возвышений и формулы), content_registry (ID),
  current_game_state (мета-цикл), CHANGELOG.

## QA-Вердикт (2026-06-12)
Статус: FAILED (3 бага)

Проверено (фактически, не по отчёту):
- Данные: `ASCENSION_MODIFIERS` — 10 кумулятивных записей, `ASCENSION_DIFFICULTY_DEFAULTS`
  нейтральны (`progression_data.gd:769-803`). `ascension_difficulty_mods()` кумулятивна:
  множители перемножаются, флаги берут max, шансы складываются (`:1002-1017`). OK.
- Unlock по персонажу: `meta_progression.record_boss_victory(state, char, run_level)` —
  +1 только при `run_level >= completed` (`:47-59`); вызов из боя передаёт
  `selected_ascension_level` (`combat_director.gd:464` → `main.gd:427-428`). Повторная
  победа на низком уровне не инфлейтит. OK.
- Уровень 0 нейтрален: дефолты 1.0/0.0, все точки применения множат/ветвят → числа не
  меняются. OK (подтверждено тестом `runtime_smoke:2699-2704`).
- Старые asc-баффы → наградный трек: `apply_ascension_bonuses` применяет earned-баффы
  через `apply_reward` + difficulty в run_modifiers (`main.gd:449-470`), без двойного
  применения. OK.
- reward_mult (L5), price_mult (L2), player_max_hp_mult (L10), healing_mult (L6),
  enemy hp/dmg (L1), spawn (L3), round_duration (L8), boss hp/phase/telegraph (L9),
  first_wave_boost (L10) — потребляются. OK.
- HUD римская цифра + тултип (`ui_screens.gd:3077-3092`), кодекс-раздел, селектор −/+ —
  присутствуют.

Тесты: `meta_progression_smoke` ✅, `runtime_smoke` ✅ (целевые проверки кумулятивности,
нулевого уровня, unlock+run_level gating, цена L2, макс-HP L10 — реальные ассерты, не
заглушки). Регрессия: все 6 smoke зелёные.

Краевые случаи: уровень 0 (нейтраль) ✅; макс открытый уровень + кнопка «+» → выявлен
баг клампа; повторная победа на уже открытом/более низком уровне → прогресс не растёт ✅.

Баги (3) — НЕ исправлялись QA, оформлены отдельными тасками:
- `bug_ascension_selector_not_clamped_task.md` (normal): кнопка «+» не клампится к
  selectable_max → можно стартовать на закрытом уровне. Нарушен критерий «селектор не
  даёт выбрать закрытый уровень».
- `bug_ascension_elite_instant_phase_dead_task.md` (high): `elite_instant_phase` мёртв —
  «фаза элитки открывается сразу» (Возвышение 4) не реализована.
- `bug_ascension_mini_elite_chance_dead_task.md` (high): `mini_elite_chance` нигде не
  потребляется — Возвышение 7 «Эхо бездны» полностью no-op (мёртвая ступень лестницы).

Вывод: ядро системы (данные, кумулятивность, unlock-гейтинг, нулевой уровень, мета-трек,
HUD) работает корректно, но 2 из 10 модификаторов (#4 частично, #7 целиком) не
реализованы, и селектор не валидирует закрытые уровни. До фикса 3 багов — FAILED.

## QA-Вердикт (повторный, 2026-06-12, релиз-гейт PM)
Статус: PASSED
Перепроверка после FAILED перед релизом 0.1.3: все ранее провалившиеся пункты
закрыты соответствующими bug-фиксами (bug-таски done), фактическая верификация:
тесты багфиксов (кламп селектора, мгновенная фаза элиток, шанс мини-элитки) входят в runtime smoke и проходят
Регрессия: все 6 тест-сьютов зелёные (runtime, animation, meta, targeting,
attack_vfx, hazard_vfx) — прогон 2026-06-12.
