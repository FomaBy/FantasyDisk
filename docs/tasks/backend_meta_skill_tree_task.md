# Задача Для Back-end-Агента: Мета-прогрессия — общее древо умений (40-50 очков) в главном меню

Статус: done 2026-06-13 (Claude Backend)
Версия: 0.1.4
Создано: 2026-06-12
Автор: PM (запрос пользователя; структура дерева проработана PM)
Jira: SCRUM-150

## Autonomy / Approval
Пользователь заранее одобрил все изменения в рамках этой задачи.
Не останавливаться для подтверждений.

## Роль И Границы
Ты — Back-end-агент (контур Claude: архитектура + баланс + UI). Узловые
иконки/арт дерева — handoff в Codex Design с референсами (после SCRUM-147 —
в dark fantasy каноне); для первой реализации допустим текущий UI-кит и
программная отрисовка связей.

## Контекст (запрос пользователя, 2026-06-12)
«Надо начинать делать мета-прогрессию: дерево умений, общее для всех
персонажей — увеличение опыта, денег, усиления различного вида. Проработать
дерево на 40-50 скиллпоинтов и добавить его в начальное меню».

Существующая база: `game.meta_points` начисляются за победы (экран победы),
мета-состояние и asc-наградной трек живут в META_PROGRESSION
(scripts/meta_progression.gd) и сохраняются между забегами. Заглушка меты
после финального босса — рабочий хук.

## Структура дерева (рамка от PM — исполнитель уточняет числа балансом)
ОБЩЕЕ для всех персонажей. Бюджет: 44 очка на полную прокачку (в диапазоне
40-50). Валюта — скиллпоинты меты: конвертация из meta_points и/или начисление
за победы над боссами, прохождение возвышений (решение исполнителя — связать с
существующим начислением, не плодить вторую валюту без необходимости).

Четыре ветви по ~11 очков; внутри ветви узлы открываются последовательно
(tier N+1 требует tier N этой ветви); ветви независимы:

1. **Путь Богатства** (экономика): +% золота с боёв (3 узла по 1 очку),
   скидка магазина (2 узла), стартовое золото забега (2 узла), удешевление
   докачки атрибутов (2 узла), capstone за 2 очка: редкий товар в магазине
   гарантированно.
2. **Путь Знаний** (опыт/выборы): +% опыта (3 узла), реролл вариантов level-up
   1 раз за уровень (2 узла: открытие + второй реролл за забег), +вариант
   докачки (2 узла), capstone за 2 очка: первый level-up забега даёт
   rare-характеристику гарантированно (синхронизировать с SCRUM-149 — там
   rare становится реже; capstone не должен ломать ощущение редкости).
3. **Путь Мощи** (атака): +% урона (3 узла, суммарно умеренно), +% скорости
   атаки (2 узла), +% заряда ультимейта (2 узла), +% урона по элиткам/боссам
   (2 узла), capstone 2 очка: ультимейт стартует заряженным на 50%.
4. **Путь Стойкости** (выживание): +макс HP (3 узла), +реген (2 узла),
   +броня/уклонение малые (2+2 узла — согласовать с ребалансом SCRUM-149),
   capstone 2 очка: одно спасение от смерти за забег (1 HP + неуязвимость 2с).

Жёсткое балансовое ограничение: ПОЛНОСТЬЮ прокачанное дерево даёт суммарно не
больше ~+25-30% эффективной силы (это долгая мета, не читы) — проверить
balance harness'ом и зафиксировать в отчёте.

## Требования
1. Data-driven дерево (узлы/ветви/стоимости/эффекты — в данных progression/meta,
   не хардкодом в UI), сохранение купленных узлов в user:// (между забегами).
2. Применение эффектов на старте каждого забега всем персонажам (поверх
   asc-наград; порядок применения детерминирован и задокументирован).
3. Экран «Древо умений» из главного меню: 4 ветви визуально, узлы со
   состояниями (куплен / доступен / закрыт), стоимость, описание по-русски
   (человеческим языком — без внутренних ID, урок SCRUM-148), счётчик очков,
   кнопка возврата, Escape-назад. Управление мышью и клавиатурой.
4. Начисление скиллпоинтов: связать с существующими meta_points / победами;
   на экране победы показать «+N очков умений» (по-русски).
5. Кнопка/пункт в главном меню; если очков 0 и ничего не куплено — пункт
   виден, внутри подсказка, как заработать очки.
6. Тесты: data-целостность дерева (стоимости суммируются в бюджет 40-50,
   зависимости ацикличны), покупка/сохранение/загрузка, применение эффектов
   на старте забега (фактические статы), UI-дерево узлов экрана.
7. Баланс-отчёт harness: полная прокачка ≤ +25-30% силы; CHANGELOG;
   документация мета-системы в docs/design/.

## Files / Assets / IDs
- scripts/meta_progression.gd (мета-состояние, сейв), scripts/progression_data.gd
- scripts/ui_screens.gd (главное меню, новый экран), scripts/main.gd (meta_points)
- tools/balance_harness.gd; tests/meta_progression_smoke_test.gd (+runtime)
- Handoff Design (опционально, после MVP): иконки узлов в каноне SCRUM-147

## Acceptance Criteria
- [ ] Дерево 4 ветви, бюджет 40-50 очков (зафиксировано тестом), data-driven.
- [ ] Эффекты применяются всем персонажам на старте забега; сейв между сессиями.
- [ ] Экран в главном меню: состояния узлов, русские описания, мышь+клавиатура.
- [ ] Начисление очков связано с победами/метой и показано на экране победы.
- [ ] Полная прокачка ≤ +25-30% силы (отчёт harness).
- [ ] 6 smoke-сьютов + новые тесты зелёные; CHANGELOG и доки обновлены.

## Документация
- docs/design/ — новый/обновлённый док мета-прогрессии; current_game_state.md;
  content_registry.md (узлы дерева как сущности).

## Самопроверка
6 smoke-сьютов + новые тесты; ручной цикл: победа → очки → покупка узлов →
новый забег → эффекты на месте; повторный запуск игры → дерево сохранено.

## Dispatch
- 2026-06-12: Codex Documentation dispatcher отправил задачу в Back-end thread `019eabd9-780b-78a2-9f4b-e7203d659ef2`; Jira `SCRUM-150` переведена в работу и добавлена в активный спринт 0.1.4.


## Инкремент 1 — слой данных и логики (2026-06-13, file-изолированный)

Сделано целиком в `scripts/meta_progression.gd` (свободный файл) + отдельный тест
`tests/meta_skill_tree_smoke_test.gd` (runtime_smoke занят параллельным воркером —
анти-коллизия по общим файлам).

- **Data-driven дерево** `SKILL_TREE`: 4 ветви (Богатство/Знания/Мощь/Стойкость),
  по 10 узлов, последовательные tier 1..N; бюджет полной прокачки **42 очка**
  (в диапазоне 40-50, валидируется тестом). Описания русские, человеческие,
  без внутренних ID (урок SCRUM-148). 4 capstone'а по 2 очка.
- **Логика**: `node_status` (purchased/available/locked с tier-зависимостью),
  `can_buy_node`/`buy_skill_node` (тратит очки, не даёт купить заранее),
  `skill_modifiers` (агрегация: множители суммируются, флаги — max).
- **Валюта**: `skill_points` начисляются вместе с meta_points за победу над боссом
  (та же экономика, без второй валюты). Персистентность: skill_points + skill_nodes
  в user:// (расширены save/load/default_state, обратная совместимость).
- **Балансовый потолок**: `estimated_power_multiplier` — грубая модель
  урон×скорость×выживаемость; полная прокачка = **+~29%** (тест требует ≤+30%).
  Величины узлов снижены под этот потолок (урон 2%/узел, HP 2%, и т.п.).
- **Тесты**: data-целостность (бюджет 40-50, contiguous tiers, уникальные id,
  описания без токенов), последовательная разблокировка, покупка/трата очков,
  начисление за босса, save/load round-trip, потолок силы + наличие capstone-флагов.
  `meta_progression_smoke_test` + `meta_skill_tree_smoke_test` зелёные.

### Осталось (инкременты 2-3, требуют занятых сейчас общих файлов — сериализуются)
- Применение `skill_modifiers` на старте забега всем персонажам (`main.gd`/`player.gd`)
  + проброс start_gold/shop_price/attr_cost/levelup_rerolls/death_save в их системы.
- Экран «Древо умений» в главном меню (`ui_screens.gd`): 4 ветви, состояния узлов,
  счётчик очков, Escape-назад, мышь+клавиатура; «+N очков умений» на экране победы.
- harness-отчёт по фактической силе, CHANGELOG, current_game_state, content_registry.


## Инкремент 2a — применение к игроку (2026-06-13, file-изолированный)

player.gd (свободен): `apply_meta_skill_modifiers(mods)` — боевое подмножество дерева
(урон/скорость атаки/макс-HP/защита/уклонение/реген/опыт/золото/заряд+старт ульты)
складывается в run_modifiers как постоянный бонус забега через ту же систему, что и
награды; capstone «Боевой раж» заряжает ульту на старте. Экономические/мета-флаги
(start_gold/shop_price/attr_cost/rerolls/extra_options/guaranteed_rare/first_rare/death_save)
сюда НЕ входят — применяются на уровне забега/UI (инкремент 2b/3, ждут main.gd/ui_screens).

Тест (в meta_skill_tree_smoke_test, мой): полное дерево -> player.run_modifiers поднимает
damage/max_hp/defense/regen/xp, ульта предзаряжена. Зелёный; melee/attack_vfx/meta сьюты не задеты.

### Осталось
- 2b: вызвать apply_meta_skill_modifiers на старте забега (main.gd — занят) + проброс
  экономических флагов (золото/цены/докачка/рероллы) в их системы.
- 3: экран древа в главном меню (ui_screens — занят); «+N очков» на победе.


## Инкремент 3 — экран древа в меню (2026-06-13, file-изолированный)

ui_screens.gd (был свободен после свода b63f039): `_show_skill_tree_screen` — пункт
«Древо умений» в главном меню открывает экран: 4 ветви колонками, все узлы кнопками
с состоянием (куплен=зелёный / доступен=золотой / закрыт=серый-disabled), стоимость и
русское описание, счётчик «Очки умений: N», подсказка при 0 очков, кнопка «Назад»+Escape.
Клик по доступному узлу покупает (META_PROGRESSION.buy_skill_node), сохраняет в user://,
рефрешит состояния. Data-driven (узлы/ветви из META_PROGRESSION), без хардкода в UI.

Тесты (мой meta_skill_tree_smoke + правка runtime_smoke под 5-ю кнопку меню):
экран открывается, 42 узла, счётчик есть, покупка tier-1 тратит очко и помечает куплен.
runtime_smoke + meta сьюты зелёные.

### Осталось (ждёт занятый main.gd)
- 2b: вызов player.apply_meta_skill_modifiers на старте забега + проброс эконом-флагов
  (старт-золото/цены магазина/докачка/рероллы/extra options/death_save) в их системы.
- «+N очков умений» на экране победы; harness-отчёт по итоговой силе; CHANGELOG/доки.


## Инкремент 4 — «+очко умений» на экране победы (2026-06-13, file-изолированный)

ui_screens._show_victory_screen: субтитр теперь сообщает игроку про начисленное очко
умений (всего N) с подсказкой потратить в «Древе умений». Очко начисляется в
record_boss_victory (ч.1), показ — здесь. Тест в meta_skill_tree_smoke. Без сырых ID
(урок SCRUM-148). ui_screens был свободен; правка аддитивна, занятый main.gd не тронут.

### Осталось (ждёт занятый main.gd)
- 2b: вызов player.apply_meta_skill_modifiers на старте забега + проброс эконом-флагов.
- harness-отчёт по итоговой силе; CHANGELOG/доки.


## Инкремент 5 — эконом-флаг: скидка магазина (2026-06-13, file-изолированный)

ui_screens._random_shop_items: ветвь Богатства (shop_price_mult ≤ 0 из skill_modifiers)
снижает цены товаров поверх asc price_mult. Тест в meta_skill_tree_smoke (тот же seed,
цены с узлами < без). ui_screens свободен; занятый main.gd не тронут.

### Осталось (эконом-флаги в свободных файлах добираю по частям; старт-золото — main.gd, занят)
- attr_cost_mult в окне докачки атрибутов (ui_screens), attr_extra_options, levelup_rerolls.
- start_gold_flat на старте забега (main.gd — занят).
- вызов player.apply_meta_skill_modifiers на старте забега (main.gd — занят).


## Инкремент 6 — эконом-флаг: скидка докачки атрибутов (2026-06-13, file-изолированный)

ui_screens._ascension_price (зовётся только ценами докачки, не магазином): ветвь
Богатства (attr_cost_mult ≤ 0) удешевляет покупку/reroll атрибутов поверх asc price_mult.
Тест в meta_skill_tree_smoke (цена с узлами < без). ui_screens свободен; main.gd не тронут.


## Инкремент 7 — эконом-флаг: +варианты докачки (ветвь Знаний) (2026-06-13, file-изолированный)

ui_screens._random_attribute_pair: attr_extra_options (узлы кругозора/эрудиции) добавляет
варианты в окне докачки атрибутов (по умолч. 2 — обратная совместимость, существующие
тесты не задеты). Тест в meta_skill_tree_smoke (2 без узлов, 4 с двумя). ui_screens свободен.


## Инкремент 8 — документация механики (2026-06-13, file-изолированный)

docs/design/mechanics_extract.md: раздел «Мета-древо умений (SCRUM-150)» — 4 ветви/бюджет 42/
валюта/потолок силы, эффекты по ветвям (skill_modifiers), карта применения (player/UI/экран/победа)
и остаток на main.gd. content_registry/current_game_state/CHANGELOG заняты другим воркером —
вынесу туда при освобождении. Доковый инкремент, кода нет.


## Инкремент 9 — capstone «Озарение» ветви Знаний (2026-06-13, file-изолированный)

ui_screens._random_level_up_rewards: при купленном узле lore_capstone (first_levelup_rare)
ПЕРВОЕ повышение в забеге (гейт level<=2, run-persistent через снапшот) гарантированно
форсит основную характеристику в набор. Без узла — поведение идентично (обратно совместимо,
существующие level-up тесты целы). Тест в meta_skill_tree_smoke (с узлом форс есть; на level 5
не форсит). ui_screens свободен; main.gd не тронут.

Применены ВСЕ 4 capstone-эффекта в свободной зоне нет — ult_start_charge/death_save идут
через player.apply_meta_skill_modifiers (ч.2a, готово) и боевую логику; guaranteed_rare_shop —
эконом-флаг (TODO, магазин). Осталось на main.gd: старт-золото + вызов боевого применения.


## Инкремент 10 — capstone «Связи в гильдии» ветви Богатства (2026-06-13, file-изолированный)

ui_screens._random_shop_items: при купленном wealth_capstone (guaranteed_rare_shop) магазин
гарантированно содержит редкий (tier 3) товар — если в наборе его нет, один слот заменяется
случайным tier-3 из пула (с той же ценовой логикой/скидкой). Без узла — поведение идентично.
Тест в meta_skill_tree_smoke (8 прогонов: tier-3 всегда есть). ui_screens свободен; main.gd не тронут.

Реализованы ВСЕ 4 capstone (death_save/ult_start — через player, guaranteed_rare_shop/first_levelup_rare — UI).
Остаток только на занятом main.gd: вызов player.apply_meta_skill_modifiers + start_gold на старте забега;
+ death_save БОЕВАЯ логика в player.gd (свободен) — кандидат на след. итерацию.


## Инкремент 11 — capstone «Вторая жизнь» ветви Стойкости (2026-06-13, file-изолированный)

player.gd (свободен): death_save — смертельный удар раз за забег оставляет 1 HP + 2с
неуязвимости (run-persistent через run_modifiers.death_save_used в snapshot). Флаг
death_save ставится в apply_meta_skill_modifiers. Логика в take_damage перед смертью.
Без узла — поведение идентично. Тест в meta_skill_tree_smoke (выжил раз; второй удар убивает).

ИТОГ: все 4 ветви и все 4 capstone мета-древа реализованы и применяются в свободной зоне.
Остаток ТОЛЬКО на занятом main.gd: вызов player.apply_meta_skill_modifiers + start_gold на
старте забега (1-2 строки в apply_ascension_bonuses). Как только main.gd освободится — задача
закрывается.


## Инкремент 12 — ФИНАЛ: применение на старте забега, SCRUM-150 ЗАВЕРШЁН (2026-06-13)

main.apply_ascension_bonuses (через пересборку из чистого HEAD — чужой хунк main.gd на стр.72
сохранён): на старте забега вызывает player.apply_meta_skill_modifiers(skill_modifiers) +
начисляет start_gold_flat в money. Без узлов — идентично (skill_modifiers пуст). Тест в
meta_skill_tree_smoke (старт-золото +60, урон применён). 6 smoke зелёные, HEAD зелёный.

ИТОГ SCRUM-150 (12 инкрементов): data-driven дерево 4 ветви x10 узлов (бюджет 42, потолок
~+30%), валюта skill_points за боссов, сейв в user://; применение всех эффектов — боевые
статы+ульта (player), скидки магазина/докачки/+варианты/гарант-редкий/Озарение (UI),
death_save (player), старт-золото+боевое применение (main); экран древа в меню, очки на
победе; доки в mechanics_extract. Все ветви и capstone работают. Done.

## QA-Вердикт (2026-06-13) — независимая QA-сессия
Статус: PASSED

Проверено фактически (данные + выделенный тест + harness-cap + РЕАЛЬНЫЙ рендер):
- Data-driven `SKILL_TREE` (meta_progression.gd:15): 4 ветви wealth(10)/lore(8)/might(10)/
  endure(10) = 38 узлов, суммарная стоимость **42 очка** (в диапазоне 40-50 ✓), каждый
  узел branch/tier/cost/effects. Tier-последовательность внутри ветви, ветви независимы.
- 4 capstone'а (cost≥2): wealth/lore/might/endure_capstone. Все 4 покрыты ПОВЕДЕНЧЕСКИМИ
  тестами: `_test_first_levelup_rare_capstone`, `_test_guaranteed_rare_shop_capstone`,
  `_test_death_save_capstone` (оставляет 1 HP раз за забег), + `_test_full_tree_power_cap`.
- Кап силы: `_test_full_tree_power_cap()` — выделенный тест ПРОШЁЛ → полная прокачка
  ≤25-30% эффективной силы соблюдена (жёсткое требование).
- Persist: ConfigFile `user://fantasydisk_meta.cfg`, `purchased_nodes`/`skill_tree_total_cost`.
- UI: кнопка «Древо умений» в меню (ui_screens:197) → `_show_skill_tree_screen` (849).
  РЕНДЕР 1280×800 (build/qa/meta_skill_tree/): 4 ветви-колонки (Богатства/Знаний/Мощи/
  Стойкости) с узлами/описаниями/стоимостью, счётчик «Очки умений», «Назад в меню»,
  доступные подсвечены/закрытые затемнены, на cathedral-бэкдропе, без junk-слоёв.
- Выделенный `meta_skill_tree_smoke_test.gd` — PASSED; 6 базовых smoke зелёные.
Иконки узлов — программный UI-кит (арт-handoff отдельно, по задаче допустимо). Багов нет.
