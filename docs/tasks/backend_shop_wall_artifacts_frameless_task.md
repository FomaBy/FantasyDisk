# Задача Для Back-end-Агента: Магазин — артефакты висят на стене без рамок

Статус: done
Версия: 0.1.4
Создано: 2026-06-12
Автор: PM (запрос пользователя со скриншотом)
Jira: SCRUM-160

Dispatcher: sent to Back-end thread `019eabd9-780b-78a2-9f4b-e7203d659ef2` on 2026-06-12.
Dispatcher: restarted to Back-end thread `019eabd9-780b-78a2-9f4b-e7203d659ef2` on 2026-06-13 after PM reset noted stale in_progress/no progress.

## Autonomy / Approval
Пользователь заранее одобрил все изменения. Не останавливаться для подтверждений.

## Роль И Границы
Ты — Back-end-агент (UI). Если решишь добавить мелкий арт-штрих «крепления»
(гвоздик/крюк/верёвочка) — handoff в Design (Codex, с референсами); сам стиль
НЕ генерируй. Допустимо обойтись и без крепления — чистые предметы на стене.

## Контекст (скриншот пользователя, 2026-06-12)
Сейчас 4 товара магазина отрисованы как карточки в массивных золотых рамках
поверх пергаментной стены — выглядит как UI-сетка, а не как лавка. Запрос:
«артефакты в магазине надо без рамок повесить на стену» — предметы должны
висеть на стене лавки как реальные товары торговца.

Код: `_show_shop_screen` (ui_screens.gd:1885-1976, ShopParchmentWall +
ShopInlineItems grid), `_make_shop_item_slot` (1980+, рамки
SHOP_SLOT_FRAME_PATH/SHOP_SLOT_HOVER_PATH ui_screens.gd:10-11).

## Требования
1. Убрать рамки-карточки: иконка артефакта (RGBA без фона — уже такие) висит
   прямо на стене. Лёгкая тень/контактное затемнение под предметом — чтобы
   «висел», а не «парил» (программно или мелкий ассет).
2. Цена — компактный ценник у предмета (монетка + число), читаемый на
   пергаменте, БЕЗ большой золотой плашки. Недоступно по золоту — ценник
   красноватый/предмет слегка затемнён.
3. Hover: мягкая подсветка/небольшое увеличение + текущий tooltip с описанием;
   зона клика не меньше прежней (даже если иконка визуально меньше).
4. Куплено: предмет исчезает со стены (снят с продажи) или остаётся силуэт/
   пустой крюк — решение исполнителя, зафиксировать.
5. Раскладка по стене: использовать зону пергамента (как сейчас anchors
   0.50-0.82 x 0.10-0.72), лёгкая «живость» (не идеальная сетка — допустим
   небольшой разброс/смещение рядов), но БЕЗ пересечений предметов и ценников
   между собой и с HUD (правило «UI не наползает» — см. qa_protocol).
6. Клавиатурная навигация и Escape («Покинуть магазин») сохраняются.
7. Тест: smoke — фактическое дерево (нет узлов рамок слотов, предметы и
   ценники присутствуют, их global_rect не пересекаются между собой и с
   верхним HUD на 1280x720 и 2560x1440).
8. Скриншот «после» в build/qa/; CHANGELOG.

## Files / Assets / IDs
- scripts/ui_screens.gd (_show_shop_screen:1885, _make_shop_item_slot:1980)
- assets/sprites/ui/shop/ (рамки слотов — выводятся из использования)
- tests/runtime_smoke_test.gd

## Acceptance Criteria
- [ ] Товары висят на стене без рамок-карточек, с ценниками и тенью.
- [ ] Hover/tooltip/покупка/недоступность/куплено — работают и читаемы.
- [ ] Ничего не пересекается (тест фактических rect на 2 разрешениях).
- [ ] Скриншот в build/qa/; 6 smoke зелёные; CHANGELOG.

## Документация
- docs/design/current_game_state.md (экран магазина).

## Самопроверка
6 smoke + скриншот/дамп дерева магазина на 1280x720 и 2560x1440.

## Result

2026-06-13: Back-end/UI scope завершен.

- `scripts/ui_screens.gd` перевел магазин с 2x2 framed card grid на frameless wall layout: кнопки остаются 164x186 для клика/клавиатуры, но визуально показывают только предмет, контактную тень и компактный ценник.
- `ShopItemButton*` больше не использует `StyleBoxTexture` frame/hover для слота; hover остается мягкой программной подсветкой.
- Цена стала компактной: money icon + число, без большой золотой плашки; недоступный товар затемняется и получает красный price tag.
- Купленный товар снимается со стены: вместо иконки/цены показывается `ShopEmptyHook` с подписью «снято».
- Runtime smoke расширен: проверяет отсутствие framed-slot styles, наличие иконки/тени/ценника/tooltip, purchase state и фактический no-overlap на 1280x720 и 2560x1440.
- QA artifact: `build/qa/shop_wall_frameless_rects.md`. Headless SubViewport screenshot недоступен в текущем dummy renderer без Godot error `Parameter "t" is null`; оставлен числовой rect-dump, как в соседних UI задачах с headless limitation.
- `CHANGELOG.md` и `docs/design/current_game_state.md` обновлены.

Verification:

- `/Users/sergeyfomin/Downloads/Godot.app/Contents/MacOS/Godot --headless --path /Users/sergeyfomin/Documents/AI\ Agent --script res://tests/runtime_smoke_test.gd` — passed.

## QA-Вердикт (2026-06-13) — независимая QA-сессия
Статус: PASSED

Проверено фактически (код + рантайм-рендер + тест):
- Рамки убраны: `_make_shop_item_slot` (ui_screens:2330) НЕ применяет SHOP_SLOT_FRAME_PATH;
  иконка висит прямо на стене (2392-2408), стиль кнопки `_shop_wall_button_style`
  (subtle, не золотая карточка). Рантайм: `frame_nodes=0`. ✓
- «Висит»: `ShopItemContactShadow` под предметом (2378-2390) — рантайм 4 тени. Тр.1 ✓.
- Компактный ценник: `ShopPriceBadge` (108×30, монета+число, 2410-2438) — рантайм 4 бейджа.
  Недоступно → иконка затемнена `Color(0.52,0.48,0.45,0.82)` (2407) + красноватый стиль
  бейджа (2422). Тр.2 ✓.
- Клик-зона = вся кнопка SHOP_INLINE_SLOT_SIZE; иконка/ценник MOUSE_FILTER_IGNORE. Тр.3 ✓.
- Куплено → disabled + `_add_shop_empty_hook` (силуэт-крюк, 2369). Тр.4 ✓.
- Тест (runtime_smoke ~4865): ассертит frameless (нет GridContainer/card layout) +
  дамп global_rect слотов для no-overlap. Тр.7 ✓.
- РЕНДЕР 1280×720 (build/qa/shop_wall_frameless/): 4 предмета на стене merchant_archive
  с ценниками+тенями, лёгкий разброс рядов, без рамок и пересечений, «Назад» внизу.
- 6 smoke зелёные. Багов нет.
