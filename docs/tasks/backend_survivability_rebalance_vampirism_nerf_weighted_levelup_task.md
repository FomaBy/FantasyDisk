# Задача Для Back-end-Агента: Ребаланс выживаемости — нерф вампиризма, усиление регена/брони/уклонения, взвешенные награды level-up

Статус: done
Версия: 0.1.4
Создано: 2026-06-12
Автор: PM (запрос пользователя)
Jira: SCRUM-149

Dispatch: отправлено в существующий Back-end чат `019eabd9-780b-78a2-9f4b-e7203d659ef2` 2026-06-12.

## Autonomy / Approval
Пользователь заранее одобрил все изменения в рамках этой задачи.
Не останавливаться для подтверждений.

## Роль И Границы
Ты — Back-end-агент (баланс). Контур Claude (балансовые решения). Только работа
своей роли.

## Контекст (запрос пользователя, 2026-06-12)
«Вампиризм сейчас работает очень имбово — взяв его, умереть нельзя. Надо сильно
уменьшить его влияние и увеличить влияние регена HP, брони и уклонения. Ещё раз
пересмотреть атрибуты — какие нужны каким персонажам. Чем больше характеристика,
от которой зависит атрибут, тем чаще он должен вылетать в наградах за уровень».

Текущее состояние (проверено PM):
- Вампиризм: `player.gd:507-511` — с шансом `vampiric_chance` (кап 0.6,
  progression_data.gd:1294) лечит `vampiric_amount + dealt_damage * 0.5`.
  Компонент «50% нанесённого урона» на высоком DPS даёт фактическое бессмертие.
- Реген/броня/уклонение: формулы в `stat_formulas.gd` (dodge:183, defense:201,
  regeneration:267) — на их фоне вампиризм строго доминирует.
- Пулы level-up: `ui_screens.gd:2444-2445` → `PROGRESSION_DATA.level_up_rewards()`
  и `main_stat_level_up_rewards()` — выбор вариантов сейчас не взвешен по
  характеристикам персонажа.

## Требования
1. **Сильный нерф вампиризма.** Убрать или жёстко ограничить компонент
   «+50% нанесённого урона» (например: лечение = vampiric_amount + малая доля
   урона с КАПОМ лечения в секунду, или убывающая отдача). Точные числа подобрать
   через `tools/balance_harness.gd`. Целевой критерий: вампиризм — поддержка
   выживания, НЕ бессмертие; в харнесс-сценарии «стоим в толпе под уроном»
   билд с максимальным вампиризмом ДОЛЖЕН проигрывать по живучести билду с
   эквивалентными вложениями в реген+броню+уклонение или быть сопоставим.
2. **Усилить реген HP, броню (defense) и уклонение (dodge)** так, чтобы они
   стали конкурентными пиками выживаемости (пересмотреть коэффициенты формул
   и величины level-up/артефактных бонусов). Капы dodge 80% / defense 75%
   пересмотреть при необходимости (вниз — допустимо, вверх — осторожно).
3. **Пересмотр атрибутов по классам.** Ещё раз пройти все атрибуты ×
   9 классов: какие кому реально полезны (сверка с derived_parameters,
   CLASS_INTERPRETATIONS и оружейными механиками). Результат — обновлённая
   таблица релевантности в данных + краткий отчёт расхождений «было/стало».
   Согласовать с задачей SCRUM-146 (ATTRIBUTE_PRIORITIES для Escape-досье):
   единый источник данных приоритетов, не две параллельные таблицы.
4. **Взвешенные награды level-up.** Вес появления атрибута в вариантах награды
   пропорционален значению характеристики персонажа, от которой атрибут зависит
   (карта зависимостей — из stat_formulas; например реген зависит от Endurance →
   у выносливого класса реген выпадает чаще). Обязательно: минимальный floor
   веса, чтобы любой атрибут мог выпасть; редкие основные характеристики
   (механика rare из level-up rework) сохраняют свою логику поверх весов.
4а. **УТОЧНЕНИЕ ПОЛЬЗОВАТЕЛЯ (2026-06-12, добавлено PM после старта): вариантов
   награды за уровень — 3, а не 5.** Переделать окно level-up и пул выдачи на
   3 варианта; обновить UI-компоновку, навигацию клавиатурой и ВСЕ тесты,
   которые ассертят 5 вариантов.
4б. **Основная характеристика в наградах — ЕЩЁ РЕЖЕ.** Шанс появления
   rare-характеристики среди вариантов заметно понизить относительно текущего
   (коэффициент подобрать, зафиксировать в данных и отчёте; ориентир — в
   несколько раз реже, выпадение характеристики должно ощущаться событием).
5. **Тесты:** unit/smoke на формулу вампиризма (кап работает), на веса пула
   (распределение смещено к высоким характеристикам, floor соблюден,
   детерминированная проверка через подменённый rng/seed).
6. **Баланс-отчёт:** прогнать `tools/balance_harness.gd` до/после,
   приложить сравнение в `build/balance_report.md`; солодамаг-бюджеты классов
   не должны уехать за ±10% (правка только выживаемости).
7. CHANGELOG (Unreleased), документация баланса/атрибутов в docs/design/.

## Files / Assets / IDs
- scripts/player.gd (on_weapon_hit:505-516)
- scripts/stat_formulas.gd (dodge, defense, regeneration, vampiric_*)
- scripts/progression_data.gd (level_up_rewards, main_stat_level_up_rewards,
  vampiric-апгрейды:671-672, leech_fang:646, клампы:1294-1295)
- scripts/ui_screens.gd (потребитель пулов:2444)
- tools/balance_harness.gd, build/balance_report.md
- tests/ (runtime/meta_progression сьюты)

## Acceptance Criteria
- [ ] Вампиризм не делает игрока бессмертным (харнесс-сценарий это показывает числами).
- [ ] Реген/броня/уклонение — конкурентные пики (сравнение в отчёте).
- [ ] Таблица атрибут×класс пересмотрена, единый источник с SCRUM-146.
- [ ] Награды level-up взвешены по характеристикам с floor; тест распределения зелёный.
- [ ] Вариантов награды за уровень ровно 3; rare-характеристика выпадает заметно реже прежнего (числа в отчёте).
- [ ] DPS-бюджеты классов в пределах ±10% от прежних.
- [ ] 6 smoke-сьютов зелёные; CHANGELOG и баланс-доки обновлены.

## Документация
- docs/design/ балансовые доки (атрибуты/выживаемость), current_game_state.md.

## Самопроверка
6 smoke-сьютов + balance harness до/после + ручной забег с вампир-билдом
(или харнесс-симуляция): смерть должна быть достижима.

## Result 2026-06-12
- Вампиризм переработан: heal = `vampiric_amount + dealt_damage * 0.08`, но лечение тратит `vampiric_heal_per_second_cap`; chance cap снижен до 35%, Leech Fang и vamp upgrades ослаблены/переведены на cap.
- Регенерация, defense и dodge усилены в `ProgressionData.derived_parameters` и `stat_formulas.gd`; level-up survival rewards подняты.
- Level-up переведен на 3 фиксированных варианта; rare main stat chance снижен до 5% на слот; обычные rewards выбираются weighted через `ProgressionData.level_up_reward_weight()` и единый `ATTRIBUTE_PRIORITIES`.
- `tools/balance_harness.gd` обновил `build/balance_report.md`; runtime smoke passed.

## QA-Вердикт (2026-06-13) — независимая QA-сессия
Статус: PASSED

Проверено фактически (код + harness + тесты + smoke на чистом HEAD):
- Нерф вампиризма (player.gd:568-583): компонент урона 0.5→**0.08** (6× слабее) +
  HARD-кап лечения `_vampiric_heal_budget` (≤4 HP/сек, накапливается cap*delta) +
  chance cap 60→35%. Бессмертие на высоком DPS исключено — лечение жёстко ограничено
  в секунду. Требования 1 ✓.
- Реген/defense/dodge усилены (derived_parameters/stat_formulas), survival-награды
  подняты (по отчёту). Требование 2.
- Взвешенный level-up: `level_up_reward_weight` (progression_data:1758-1762) =
  base × `attribute_priority_weight` с floor `maxf(0.25,...)`; единый `ATTRIBUTE_PRIORITIES`
  (унификация с SCRUM-146). Требования 3,4 ✓.
- 3 варианта (не 5): `_random_level_up_rewards(3)` (ui_screens:1813,2655); rare
  main-stat `MAIN_STAT_SLOT_CHANCE := 0.05` (было 0.13, ~в 2.6× реже). Тест «ровно 3
  варианта» (runtime_smoke:791). Требования 4а,4б ✓ (пересматривает прежний 5-вариантный
  SCRUM-114).
- Баланс: After-Tuning 51 строка — ВСЕ солодамаг-отклонения в пределах ±10% (≈0%):
  правка только выживаемости, DPS-бюджеты не уехали. Требование 6 ✓.
- 6 smoke зелёные на ЧИСТОМ worktree HEAD. Багов нет.
