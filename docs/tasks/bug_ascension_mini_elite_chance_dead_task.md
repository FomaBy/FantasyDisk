# BUG: Возвышение 7 «Эхо бездны» полностью не реализовано — mini_elite_chance dead (уровень = no-op)

Статус: done 2026-06-12. Фикс: combat_director._maybe_spawn_mini_elite читает game.ascension_difficulty()[mini_elite_chance] и с этим шансом в обычной волне спавнит мини-элитку (реальная elite-сцена, HP x0.55 от волнового elite-скейла — убиваемая) + свита 1-2 обычных врага. Вызов в начале _spawn_enemy_wave для не-боссовых/не-элитных боёв, учитывает remaining_slots. Тест: уровень 7 даёт mini_elite_chance>0. Smoke зелёный.
Приоритет: high
Роль: Back-end
Jira: SCRUM-60
Найдено QA при тестировании: `docs/tasks/qa_review_backend_ascension_difficulty_ladder_task.md`
(исходная фича: `docs/tasks/backend_ascension_difficulty_ladder_task.md`)

## Воспроизведение
1. Старт забега на Возвышении 7.
2. Играть обычные (не-боссовые) волны.
3. Наблюдать: мини-элитки со свитой в обычных волнах НЕ появляются. Сложность
   уровня 7 не отличается от уровня 6.

## Ожидание / Реальность
- Ожидание (модификатор уровня 7): «В обычных волнах может появиться мини-элитка
  со свитой» (`mini_elite_chance = 0.20`).
- Реальность: ключ `mini_elite_chance` существует только в
  `scripts/progression_data.gd:783` (определение) и `:799` (нейтральный дефолт).
  Нигде в `scripts/` не потребляется (grep `mini_elite` — только эти 2 строки).
  Так как это ЕДИНСТВЕННЫЙ модификатор уровня 7, переход 6→7 не добавляет
  сложности вообще — мёртвая ступень лестницы.

## Влияние
Целая ступень возвышения (7) — no-op. Нарушает критерий «10 кумулятивных
усложнений работают». Особенно заметно: лестница «ровная» на 6↔7.

## Предлагаемое направление фикса (для исполнителя)
В спавне обычных волн (`combat_director.gd`) читать
`game.ascension_difficulty()["mini_elite_chance"]` и с этой вероятностью спавнить
мини-элитку со свитой. Добавить тест на фактический эффект.

## Окружение
Godot 4.6.3.stable. Коммит 8d5e489 (+ незакоммиченные правки). Класс — любой,
уровень возвышения ≥ 7.


## QA-Вердикт

QA: passed 2026-06-12. Фикс combat_director._maybe_spawn_mini_elite (176-207) верифицирован: тройная защита (chance>0, remaining_slots>=2, rng), все 7 вызываемых helper'ов существуют, HP x0.55 (убиваемая), свита ограничена `remaining_slots-1`, слоты корректно вычитаются из волны. Наблюдение (не баг): ролл per-wave — при L7 (20%) мини-элитка может выпадать на каждой волне; осознанное поведение модификатора сложности. runtime_smoke (mini_elite_chance>0 на L7) + сьюты зелёные.

> Независимость рецензента: автор фикса и QA — один экземпляр Claude (в этом workflow роль «QA (Claude)» исполняется тем же агентом). Ревью проведено придирчиво (adversarial чтение + повторный прогон всех сьютов), но полностью независимым его считать нельзя — отмечено для прозрачности.
