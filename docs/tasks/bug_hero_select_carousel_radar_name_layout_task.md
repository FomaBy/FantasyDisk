# BUG: Выбор героя — лишние подписи в карусели, позиция розы ветров, дубль имени

Статус: done 2026-06-12 (Claude Backend)
Приоритет: high
Роль: Back-end (UI)
Создано: 2026-06-12
Автор: PM (отчет пользователя)
Jira: SCRUM-140

Dispatch: отправлено в существующий Back-end чат `019eabd9-780b-78a2-9f4b-e7203d659ef2` 2026-06-12.

## Контекст
Экран выбора героя v3 (портрет слева + досье + карусель снизу) в целом нравится
пользователю, но есть дефекты компоновки.

## Воспроизведение
Открыть выбор героя.

## Ожидание / Реальность (3 пункта)
1. **Карусель снизу**: должна содержать ТОЛЬКО картинки-миниатюры героев —
   сейчас под/на миниатюрах есть названия. Убрать текст из карусели полностью
   (имя видно в тултипе при hover — опционально).
2. **Роза ветров характеристик**: должна быть в ПРАВОМ ВЕРХНЕМ углу экрана —
   сейчас стоит в другом месте досье. Перенести в правый верхний угол,
   читаемость на 1280x720 сохранить.
3. **Имя персонажа**: должно быть ТОЛЬКО на фрейме с деталями (досье) —
   сейчас дублируется под внешним видом персонажа (под портретом слева).
   Подпись под портретом убрать.

## Требования
- Ничего больше в компоновке не менять (портрет слева во всю высоту, досье,
  селектор возвышения — как есть).
- Проверить 1280x720 и 2560x1440: тексты в рамках, роза не перекрывает досье.
- Скриншоты до/после в build/qa/ или лог задачи.
- Smoke зеленые (+ обновить проверки layout, если они ассертят старые позиции).

## Окружение
dev, экран выбора героя v3 (после backend_hero_select_portrait_left_radar_task).


## Исправление — 2026-06-12 (SCRUM-139)

Все 3 дефекта компоновки hero-select v3 (`_show_character_select`, ui_screens.gd):
1. **Карусель — только картинки**: из `_make_hero_thumbnail_button` убран текстовый Label (`HeroThumbnailTitle_*`), миниатюра-портрет растянута на всю кнопку; имя по-прежнему в тултипе при hover.
2. **Роза ветров — правый верхний угол**: `HeroStatRadar` вынесена из VBox досье и стала плавающим виджетом на корне `HeroSelectScreen` (anchor справа/сверху, offset_left -344, offset_top 88, z_index 6) — не перекрывает текст досье (он выровнен влево).
3. **Имя только в досье**: удалён дублирующий Label `HeroSelectPortraitName` под портретом слева (и его установка в select_character); имя остаётся в `HeroSelectInfoTitle` (досье).

Остальная компоновка не тронута (портрет слева во всю высоту, досье, селектор возвышения). Тест runtime_smoke обновлён ассертами: роза — top-right floating (parent=root, anchor_left≥0.99, offset_left≤-280, offset_top≥70), нет `HeroSelectPortraitName`, в миниатюрах нет Label'ов. Все 6 smoke зелёные.

_Скриншоты:_ windowed Godot в этом окружении не выдаёт offscreen-кадр — компоновка подтверждена автотестом (анкоры/наличие/отсутствие узлов); пиксельная сверка 1280x720/2560x1440 — за плейтестом.
