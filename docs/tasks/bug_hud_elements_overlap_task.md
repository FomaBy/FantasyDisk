# BUG: Элементы HUD наползают друг на друга (таймер на панель ULT)

Статус: done
Приоритет: high
Роль: Back-end (UI)
Создано: 2026-06-12
Автор: PM (отчет пользователя со скриншотом)
Jira: SCRUM-161

Dispatcher: sent to Back-end thread `019eabd9-780b-78a2-9f4b-e7203d659ef2` on 2026-06-12.

## Autonomy / Approval
Пользователь заранее одобрил все изменения. Не останавливаться для подтверждений.

## Контекст
Скриншот пользователя (бой, широкое окно): панель таймера (центр-верх)
НАПОЛЗАЕТ на панель ULT (правый край левого ресурсного HUD). Ресурсный HUD
анкорится слева (ui_screens.gd:3646, Vector2(20,18)), таймер — к центру
(anchor 0.5, offset ±86): при недостаточной ширине окна зоны пересекаются.
Также проверить правый верх: ряд иконок артефактов (ArtifactHudRow, правый
край) может пересекаться с HP-барами врагов — это world-space, не чинить, но
плашки HUD между собой пересекаться не должны.

Одновременно вводится ПРАВИЛО (см. qa_protocol.md): элементы интерфейса не
должны наползать друг на друга — на любом поддерживаемом разрешении.

## Воспроизведение
1. Запустить бой в окне, где (ширина/2 - 86) < правый край ресурсного HUD
   (например ~1900x573 как на скриншоте, или 1280x720 — проверить).
2. Таймер перекрывает панель ULT.

## Требования
1. Починить верхний HUD: ресурсная панель, таймер, бейдж возвышения, ряд
   артефактов НЕ пересекаются ни при каком поддерживаемом размере окна
   (минимум 1152x648/1280x720 до ultrawide). Решение — на выбор исполнителя:
   единый HBox-лейаут верхней полосы, либо таймер уступает (сдвиг вправо от
   ресурсной панели при нехватке места), либо адаптивное сжатие ресурсной
   панели. Зафиксировать выбор в отчёте.
2. **Общий тест-хелпер «no-overlap»**: функция, собирающая видимые top-level
   Control-плашки HUD и ассертящая попарную непересекаемость их фактических
   `global_rect` (допуск 0-2px). Прогнать в smoke для боевого HUD на
   3 размерах окна (1152x648, 1280x720, 2560x1440) и для боя с боссом
   (там вместо таймера — бейдж возвышения).
3. Хелпер сделать переиспользуемым для других экранов (магазин, hero select)
   — подключение к остальным экранам можно отдельным шагом, но API готово.
4. CHANGELOG; скриншот/дамп rect'ов в build/qa/.

## Files / Assets / IDs
- scripts/ui_screens.gd (_create_hud:3640+, _create_resource_hud_panel,
  _create_combat_timer_panel, _create_artifact_hud_row)
- tests/runtime_smoke_test.gd (+ хелпер no-overlap)

## Acceptance Criteria
- [ ] На 3 размерах окна верхний HUD без пересечений (тест фактических rect).
- [ ] Хелпер no-overlap переиспользуем, подключён к боевому HUD.
- [ ] 6 smoke зелёные; CHANGELOG; артефакт проверки в build/qa/.

## Документация
- docs/design/current_game_state.md (HUD): правило непересечения.

## Окружение
dev; скриншот пользователя: широкое низкое окно, элитка-голем слева, таймер
0:12 поверх правого края ULT-панели.

## Result
- 2026-06-12: Back-end done. Исправление выбрано как adaptive top HUD layout, а не ручной сдвиг одного таймера.
- `RunResourceHud` теперь реально сжимается на малых ширинах; внутренние карточки HP/XP/Money/ULT уменьшены по min-size, чтобы фактический `global_rect` освобождал место.
- `CombatTimerPanel` остается в верхней полосе: на широких окнах близко к центру, на узких уступает ресурсной панели и смещается вправо. `AscensionHudBadge` занимает соседнюю свободную позицию.
- `ArtifactHudRow` остается справа сверху на широких окнах и переносится ниже (`y=88`) на 1152x648, если верхняя полоса занята.
- Runtime smoke добавил reusable helpers `_visible_hud_top_controls()`, `_first_control_overlap()`, `_rect_with_tolerance()` и проверяет battle/boss HUD на 1152x648, 1280x720, 2560x1440 по фактическим `global_rect`.
- QA dump: `build/qa/hud_no_overlap_rects.md`.
- Verification: runtime smoke passed.

## QA-Вердикт (2026-06-12) — независимая QA-сессия
Статус: PASSED

Проверено фактически:
- Тест-хелпер no-overlap РЕАЛЬНЫЙ: `_visible_hud_top_controls` + `_first_control_overlap`
  (попарная проверка фактических global_rect с допуском 2px) +
  `_rect_with_tolerance` (runtime_smoke:3807-3832), переиспользуемое API. Прогон на
  3 размерах (1152x648 / 1280x720 / 2560x1440) для battle И boss HUD (:3759). VERIFIED.
- Dump rect'ов `build/qa/hud_no_overlap_rects.md` подтверждает зазоры на всех 6
  конфигурациях (напр. 1152: ресурсная 18+576=594 < таймер 608; таймер 608+172=780 <
  бейдж 788; ArtifactHudRow перенесён на y=88). VERIFIED.
- Репро-кейс пользователя (широкое низкое окно ~1900px): адаптив держит — ресурсная
  панель ≤750px (правый край ~768) < центр-офсет таймера (1900/2−86=864). VERIFIED по
  геометрии dump.
- Визуальный контроль: реальный рендер боевого HUD — ресурсная панель и таймер с
  чистым зазором, наползаний нет. Скрин: `build/qa/hud_no_overlap/combat_hud_narrow_after.png`.
- runtime_smoke (включая no-overlap тест) ✅; все 6 smoke зелёные.

Багов нет. Правило «UI не наползает» закреплено тестом.
