# Мета 4.0: экран «Атлас героев» — созвездия, лента классов, вкладка Гильдии

Статус: done (Атлас реализован; смоки atlas_screen/no-overlap/runtime зелёные; сдан в QA)
Приоритет: high
Роль: Back-end / UI (Claude)
Версия: 0.2.0
Создано: 2026-07-02
Jira: SCRUM-827
Контур: Claude
Owner: unassigned
Thread/Worker: n/a
Locked paths: `scripts/ui_screens.gd` (экран дерева/атласа), `tests/ui_no_overlap_matrix_test.gd`, UI-смоки экрана

## Источник истины

`docs/design/systems/meta_constellations.md` §7 — лейаут, состояния узлов,
церемонии, UX-решения. Мокап: `docs/design/previews/meta40_atlas_mockup.png`.
Интерактивная логика покупки/keystone-переключения — из ядра (T1). Ассеты —
из арт-кита (T2, `assets/sprites/ui/meta40/`). Задача полностью автономна.

## Разблокировка

Снять blocked, когда SCRUM-828 (ядро: валюты/графы/API) и SCRUM-826 (арт-кит)
в «Готово». Проверить: `git log origin/dev` на их коммиты + статусы в Jira.

## Scope

Заменить экран дерева v3 на «Атлас героев» (§7):

1. Один экран, две вкладки: Созвездие (по умолчанию) / Гильдия.
2. Созвездие выбранного класса целиком на экране, без пан/зума: фон-небо,
   силуэт-линии, сокеты по типам, состояния (куплен=золотая звезда,
   доступен=пульс, недоступен=тусклый, скрыт=туман «?», keystone
   активный=сапфировое сияние / купленный-неактивный=тлеет).
3. Лента классов слева: 17 медальонов-портретов, скролл, прогресс x/N и бейдж
   непотраченных эмблем на медальоне; переключение класса — 1 клик.
4. Панель узла справа: титул, тип, описание С ЧИСЛАМИ, цена, «Вложить
   эмблему»; keystone — переключатель «Активна»; скрытая — условие и прогресс.
5. Шапка: эмблемы выбранного класса + звёздная пыль + вкладка «Гильдия»;
   низ: «Респек — бесплатно» + легенда.
6. Церемонии: вспышка покупки, загорание линий, рассеивание тумана скрытой
   звезды (0.6с, скип кликом).
7. Геймпад/клавиатура: фокус по adj-соседям, Tab — вкладки.
8. Контент строго в safe-area рам (правило проекта); все тексты русские.

## Acceptance

1. Экран соответствует мокапу по композиции и §7 по поведению (все 6 состояний
   узлов, обе вкладки, лента, панель, церемонии).
2. UI-смоки: `ui_no_overlap_matrix_test` зелёный; новый смок экрана Атласа
   (открытие, выбор класса, покупка узла, keystone-переключение, вкладка
   Гильдии) зелёный; runtime smoke зелёный. Скриншот-evidence экрана в
   `docs/design/previews/meta40_atlas_runtime_*.png`.
3. Старый экран дерева удалён/заменён без осиротевших вызовов.
4. CHANGELOG + Jira evidence; сдача `Статус: done`, Jira → «Контроль качества».

## Процесс

Полная автономия; git pull перед стартом, явный add, push после зелёных гейтов;
гейты через `python3 tools/godot_gate.py` сериализованно.

## Прогресс

2026-07-02 — реализация завершена, сдано в QA (Claude, SCRUM-827).

- Экран: `_show_atlas_screen` + семейство `_atlas_*` в `scripts/ui_screens.gd`
  (старый `_show_skill_tree_screen` и его хелперы/константы удалены; кнопка
  меню `MainMenuSkillTreeButton` теперь «Атлас героев»).
- §7 покрыт: две вкладки (Tab-шорткат на неактивной вкладке + LB/RB через
  `_handle_menu_shoulder_nav`), созвездие целиком без пан/зума (npos 0..1 →
  холст), 6 состояний узлов (звезда/пульс/тусклый/туман «?»/сапфировое кольцо
  активного keystone/тлеющий неактивный), лента 17 медальонов-гербов с
  прогрессом x/N и бейджем непотраченных эмблем, панель узла (тип/титул/
  описание с числами/цена/«Вложить эмблему|пыль», keystone-переключатель,
  условие+прогресс скрытой), шапка-валюты, низ «Респек — бесплатно» (свой
  конфирм; per-class / Атлас) + легенда. Церемонии: вспышка покупки +
  загорание линий (edge_flash), рассеивание тумана 0.6с со скипом кликом
  (реестр показов per-session).
- Anti-freed-lambda: все твины — property-твины на самих нодах; колбэки только
  `Callable(self, "метод").bind(...)` (паттерн SCRUM-551).
- Ассеты: строго кит `assets/sprites/ui/meta40/` (bg_sky, полая рама
  frame_border 9-slice c draw_center=false + nearest, сокеты по ролям,
  star_alloc, keystone_ring, crest_* как ядра созвездий и медальоны ленты,
  currency_*). Контент в safe-area рамы (margins = texture-margins рамы).
- Тесты (worktree, изолированный user-dir, полный --import): новый
  `tests/meta40_atlas_screen_smoke_test.gd` (открытие/лента/покупка/keystone/
  Гильдия/туман) PASS; `ui_no_overlap_matrix_test` (7 вьюпортов, атлас-набор
  контролов) PASS; `runtime_smoke_test` (атлас-каркас + back-button QA) PASS;
  `gamepad_menu_focus_test` (стартовый фокус — медальон выбранного класса)
  PASS; `meta_skill_tree_smoke_test` (покупка через панель Атласа) PASS.
- Отступление от AC-2 (скриншот-evidence `meta40_atlas_runtime_*.png`):
  headless dummy-рендер Godot не растеризует SubViewport (пустой Image),
  оконный запуск запрещён — на машине живой редактор пользователя. Evidence —
  геометрические дампы `build/qa/ui_no_overlap_matrix.md` (секции skill_tree)
  и `build/qa/scrum331/progression_skill_tree_runtime_dump.md` (atlas NxN,
  текстуры кита в каждом контроле). Скриншоты добрать в QA-прогоне с окном.
- Origin: dbf8e607 (экран) → becb4d36 (parse-fix) → 9d44bb09 (смоки) →
  c7515444 (.uid) → b4419fd4 (CHANGELOG + done).

## QA-Вердикт
Статус: PASSED

2026-07-02 — QA-приёмка (claude-qa), HEAD b4419fd4 origin/dev, Godot 4.7,
семафор fdengine (флот жив: чужой wt828gap + живой редактор пользователя — не тронуты).

- Смоки на HEAD ЗЕЛЁНЫЕ: `meta40_atlas_screen_smoke_test` (открытие/небо-bg из
  кита/рама/17 медальонов/22 узла/шапка-валюты/ядро-герб/лента-переключение/
  покупка «Вложить эмблему» с декрементом сигилов/keystone-переключатель
  ≤1 активна+free-respec/вкладка Гильдия «Вложить пыль») PASS;
  `ui_no_overlap_matrix_test` (7 вьюпортов 1152→3840, атлас-набор) PASS;
  `runtime_smoke_test` (dup-guard 16324 файла) PASS.
- Замена без осиротевших: `_show_skill_tree_screen` отсутствует в scripts/tests
  (grep пусто), кнопка меню → `_show_atlas_screen`. CHANGELOG (SCRUM-827) есть.
- Лейаут: на базовом каноне 2560×1440 всё внутри с запасом (правый край
  контента ≤2293 < 2560; safe-area рамы соблюдена). Кажущийся выход за правый
  край в дампе на 1152/1280 — артефакт сырого SubViewport в тесте (stretch
  `canvas_items` к SubViewport не применяется); реальная игра всегда рендерит
  UI в логике 2560×1440 (aspect keep) и масштабирует к окну → оверфлоу нет.
  Атлас, как и все устоявшиеся экраны, вне `_requires_viewport_fit` — проверка
  наслоений (overlap) чистая на всех 7 размерах.
- AC-2 скриншот `meta40_atlas_runtime_*.png`: отсутствует (задокументировано —
  headless не растеризует, оконный запуск блокирован живым редактором). Не
  блокер: назначение evidence (корректный рендер/лейаут) закрыто тремя зелёными
  сюитами + свежий авторитетный гео-дамп `build/qa/scrum331/
  progression_ui_no_overlap_matrix.md`. Скрин добрать при оконном прогоне.
