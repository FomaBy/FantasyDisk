# TASK: Поведенческий тест на спавн мини-элитки (Возвышение 7), а не только data-check

Статус: done 2026-06-12
Приоритет: low
Роль: Back-end
Создано: 2026-06-12 (QA-агент, при независимой перепроверке фикса
`bug_ascension_mini_elite_chance_dead_task.md`)

## Контекст
Фикс Возвышения 7 (`combat_director._maybe_spawn_mini_elite`, 176-207) корректен —
проверено adversarial-чтением кода QA. НО добавленный тест
(`tests/runtime_smoke_test.gd:2811-2812`) проверяет ТОЛЬКО данные:
`ascension_difficulty_mods(7)["mini_elite_chance"] > 0.0`. Фактический спавн
(мини-элитка появляется, HP×0.55, свита 1-2, корректный учёт слотов) тестом НЕ
покрыт.

Это повторяет ровно тот паттерн, из-за которого исходный баг и проехал в `done`:
данные были, потребления не было, а data-only тест давал ложную уверенность.
(Для сравнения: фикс instant_phase получил настоящий поведенческий тест,
`runtime_smoke_test.gd:2790-2805` — образец.)

## Что сделать
Добавить поведенческий тест: при `mini_elite_chance` форсированном в 1.0 и
достаточных `remaining_slots`, вызов `_maybe_spawn_mini_elite` (или прогон
`_spawn_enemy_wave` в обычной волне) должен:
- создать узел в группе `elite_enemies`;
- его `max_health` ≈ волновой elite-HP × 0.55 (убиваемый, не танк);
- вернуть число занятых слотов = 1 + размер свиты;
- не превышать `remaining_slots`.
Использовать детерминированный rng (как соседние ascension-тесты).

## Acceptance
- [x] Тест падает на «мёртвой» версии (PART A: при отключённом потреблении `_spawn_enemy_wave` не растит группу `elite_enemies` — проверено симуляцией) и проходит на текущей.
- [x] runtime smoke остаётся зелёным.

## Окружение
Godot 4.6.3.stable. Фикс закоммичен (см. историю `dev`).


## Результат

Добавлен поведенческий тест в `tests/runtime_smoke_test.gd` (_test_ascension_difficulty_ladder):
- **(A) путь потребления**: `_spawn_enemy_wave` при forced `mini_elite_chance=1.0` обязан создать узел в группе `elite_enemies` — ловит исходную регрессию «данные есть, потребления нет» (подтверждено: на симуляции мёртвой версии тест падает).
- **(B) прямой вызов** `_maybe_spawn_mini_elite(asc, 5)`: ровно 1 мини-элитка в `elite_enemies`; учёт слотов `enemies==used` (мини + свита 1-2); `used` в диапазоне 2..5 (не превышает `remaining_slots`); HP = волновой elite-скейл × 0.55 (сверка с эталонной копией той же сцены, допуск 3%) — убиваемая, не танк.
Детерминированный rng (seed 24607). Все smoke зелёные.

## QA-Вердикт (2026-06-12)
Статус: PASSED (закрыта PM при релизной зачистке)
Тестовая задача: её содержимое — сами тесты, входят в runtime smoke и зелёные при релиз-гейте.
