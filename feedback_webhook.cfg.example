; FantasyDisk — локальный dev-оверрайд вебхука внутриигрового фидбека.
; Webhook является credential и не встраивается в клиент. Без явной настройки
; отчёты безопасно сохраняются в user://feedback.
; скопируй в feedback_webhook.cfg (в .gitignore) и вставь свой Discord webhook URL.
; Формат Godot ConfigFile — комментарии через ';', значение в кавычках.
[feedback]
discord_webhook_url="https://discord.com/api/webhooks/XXXX/YYYY"
