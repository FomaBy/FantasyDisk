**Sparse-coverage CI repair published as a new candidate.** `candidate_sha` = `dispatch_candidate_sha` = `qa_candidate_sha` = `92c0810b5f64366c956fa86c5c3d2d47b6214026`, tree `2f34df1b4d7f500049b2a92ec7fe6970b1bca87d`, branch `fan3934-ci-recovery` (additive successor; approved source `384f69be` and its exact QA PASS record preserved as history — per your decision this workflow change creates a new candidate requiring fresh independent review). Sole author/exclusion ID `11dd50f9-2322-4aa0-a09e-631e1159c1df`. Changed paths vs `384f69be`: `.github/workflows/quality.yml` (cone entry + explanatory comment only), `tests/test_quality_workflow.py` (contracts), `evidence/p3-object-budget-rework/ci-atlas-diagnosis/**`. Product code, tracked fragment bytes, thresholds, job names, event profiles, engine pins, history/LFS handling, suite selection and the finite timeout are untouched.

**The repair:** the candidate job's sparse cone now includes `docs/design/reports/fan1438_a5_balance/fragments` — the whole subtree, materializing the `ultimate_atlas` input the failed suite reads and closing the identical latent `offensive` gap — while retaining every existing entry (the two previously explicit convergence fixtures are subsumed by the subtree entry).

**Contracts:** a new regression asserts the corrected cone covers both required fragment inputs (files verified tracked on disk) and includes a failure case reconstructing the pre-repair cone (explicit conditional/defensive files only) and proving it misses the subtree; the existing materialization contract now accepts the subtree entry (with directory-capable existence checks) instead of the two subsumed file lines.

**Repair validation (the decisive check, not a full-checkout pass):** isolated blob-filtered checkout at the exact synthetic merge `6d454ed7…`, the repaired workflow's exact cone applied via `.git/info/sparse-checkout` — both the ultimate_atlas and offensive fragments **present** under sparse, and `ultimate_atlas_attribution_test` via the Godot gate → **exit 0, PASS** (`corrected-cone-suite-pass.log`). The pre-repair failure at the same commit remains recorded in `sparse-cone-suite-fail.log`.

**Diagnostic evidence obligation completed:** `ci-atlas-diagnosis/MANIFEST.md` publishes the previously missing SHA-256 manifest — hashes of the diagnosis report and both original reproduction logs bound to the preserved diagnostic branch commit `c28336c8…`, the new corrected-cone validation log, exact commands/environment, before/after fragment observations (absent→present with the working-tree hash matching the full-checkout control), and an explicit statement that no in-CI hash observation exists (the failed job log records only the existence-assertion error — unavailable, not asserted).

**Final verification on `92c0810b`:** `python3 tools/quality_gate.py --static-only --changed-ref origin/dev` → **QUALITY PASSED: 16 static, 0 Godot**; `tests.test_quality_workflow` + `tests.test_quality_static_guard` → OK; `git diff --check origin/dev...HEAD` → clean; cross-domain declaration present in both commits.

Metadata pins written and read back; status `in_progress`. This new candidate requires fresh independent macOS qa_high, then protected CI PASS for the exact SHA and dedicated serial unchanged-content integration into `dev`.
