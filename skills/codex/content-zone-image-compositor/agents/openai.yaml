interface:
  display_name: "Content Zone Image Compositor"
  short_description: "Plan UI art around fixed content zones"
  default_prompt: "Use $content-zone-image-compositor to plan a strict dark fantasy UI element, validate fit/scrollbars, generate PixelLab MCP art around fixed zones, and render content only inside them."
