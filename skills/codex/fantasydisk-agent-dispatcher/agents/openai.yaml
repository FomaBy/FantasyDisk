interface:
  display_name: "FantasyDisk Agent Dispatcher"
  short_description: "Dispatch race-safe Multica FantasyDisk agents"
  default_prompt: "Use $fantasydisk-agent-dispatcher to discover FantasyDisk FAN issues, assign one owner per issue, verify delivery, and keep the queue moving."
