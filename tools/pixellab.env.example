# PixelLab local auth template.
# Copy values into a local ignored .env or shell profile. Never commit real tokens.

# Preferred: set only the raw bearer token locally.
PIXELLAB_BEARER_TOKEN=

# Alternative: set the full header locally instead of PIXELLAB_BEARER_TOKEN.
# AUTH_HEADER=Bearer <secret>
